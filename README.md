# word-prediction
This project implements a next-word prediction model using LSTM and GRU neural networks, trained on the Hamlet dataset. The goal is to learn sequential word patterns and compare the performance of both architectures in language modeling tasks.
